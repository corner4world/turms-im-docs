import{_ as e,c as t,o as s,R as n}from"./chunks/framework.b6850781.js";const f=JSON.parse('{"title":"Distribution","description":"","frontmatter":{},"headers":[],"relativePath":"server/deployment/distribution.md"}'),a={name:"server/deployment/distribution.md"},o=n(`<h1 id="distribution" tabindex="-1">Distribution <a class="header-anchor" href="#distribution" aria-label="Permalink to &quot;Distribution&quot;">​</a></h1><h2 id="the-directory-structure-of-server-release-package" tabindex="-1">The Directory Structure of Server Release Package <a class="header-anchor" href="#the-directory-structure-of-server-release-package" aria-label="Permalink to &quot;The Directory Structure of Server Release Package&quot;">​</a></h2><p>The directory structure of the turms-gateway and turms-service server release packages is as follows:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">├─bin</span></span>
<span class="line"><span style="color:#A6ACCD;">│ └─run.sh</span></span>
<span class="line"><span style="color:#A6ACCD;">├─config</span></span>
<span class="line"><span style="color:#A6ACCD;">│ ├─application.yaml</span></span>
<span class="line"><span style="color:#A6ACCD;">│ └─jvm.options</span></span>
<span class="line"><span style="color:#A6ACCD;">├─hprof</span></span>
<span class="line"><span style="color:#A6ACCD;">├─jdk</span></span>
<span class="line"><span style="color:#A6ACCD;">│─jfr</span></span>
<span class="line"><span style="color:#A6ACCD;">│─lib</span></span>
<span class="line"><span style="color:#A6ACCD;">│ ├─turms-gateway.jar (or turms-service.jar)</span></span>
<span class="line"><span style="color:#A6ACCD;">│ └─... jar</span></span>
<span class="line"><span style="color:#A6ACCD;">│─log</span></span>
<span class="line"><span style="color:#A6ACCD;">└─plugins</span></span></code></pre></div><table><thead><tr><th>directory</th><th>must exist</th><th>role</th></tr></thead><tbody><tr><td>bin</td><td>no</td><td>Holds executable scripts. run.sh is used to read the context configuration and start the Turms server</td></tr><tr><td>config</td><td>yes</td><td>Store configuration files. <br>application.yaml is used to override and add application layer configuration (such as Spring, Turms and other configurations); <br>jvm.options is used to set JVM configuration. Normally, you don&#39;t need to modify this file directly, but add JVM configuration through the environment variable <code>TURMS_GATEWAY_JVM_OPTS</code> (or <code>TURMS_SERVICE_JVM_OPTS</code>)</td></tr><tr><td>hprof</td><td>no</td><td>store heap dump snapshots</td></tr><tr><td>jdk</td><td>no</td><td>Store the JDK. The bin script uses the JDK under <code>JAVA_HOME</code> first. If you do not set the <code>JAVA_HOME</code> environment variable, use the JDK under this directory</td></tr><tr><td>jfr</td><td>No</td><td>Store JFR real-time flight record information</td></tr><tr><td>lib</td><td>Yes</td><td>Store runtime Jar package dependencies, excluding custom plug-in implementations</td></tr><tr><td>log</td><td>No</td><td>Store logs (including GC logs, API call logs, application logs, etc.)</td></tr><tr><td>plugins</td><td>No</td><td>Jar package dependencies for storing plugins. Note that the Turms server will only detect whether the JAR package ending with <code>jar</code> in the <code>plugins</code> directory is a plug-in implementation, so if you put the plug-in JAR package in the <code>lib</code> directory, these plug-ins will not be recognized. use</td></tr></tbody></table><p>Note: The environment variable <code>TURMS_GATEWAY_HOME</code> (corresponding to the turms-gateway server) or <code>TURMS_SERVICE_HOME</code> (corresponding to the turms-service server) is very important for the run.sh script and the turms server to read and store data correctly. If you run the Turms server through run.sh or a Docker image, and you do not set the above environment variables, the run.sh script will automatically deduce the HOME directory location. If you do not run it through the above method (such as starting directly through the IDE), it is recommended that you manually configure the <code>TURMS_GATEWAY_HOME</code> or <code>TURMS_SERVICE_HOME</code> environment variable, otherwise the Turms server will use <code>.</code> (current directory) as the HOME environment.</p><h2 id="docker-images" tabindex="-1">Docker Images <a class="header-anchor" href="#docker-images" aria-label="Permalink to &quot;Docker Images&quot;">​</a></h2><p>It is strongly recommended that you use a Docker image to deploy the Turms server.</p><p>At present, the version number of the Docker image on the Turms server is <code>latest</code>, that is, the image with a specific version number is not provided for the time being. The specific command to pull the image is as follows:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">docker</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">pull</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">ghcr.io/turms-im/turms-admin:latest</span></span>
<span class="line"><span style="color:#FFCB6B;">docker</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">pull</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">ghcr.io/turms-im/turms-gateway:latest</span></span>
<span class="line"><span style="color:#FFCB6B;">docker</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">pull</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">ghcr.io/turms-im/turms-service:latest</span></span></code></pre></div><h2 id="custom-publish-artifacts" tabindex="-1">Custom Publish Artifacts <a class="header-anchor" href="#custom-publish-artifacts" aria-label="Permalink to &quot;Custom Publish Artifacts&quot;">​</a></h2><p>If users don&#39;t want to use the official products provided by Turms (such as Docker image), they can also generate the products they need according to the source code and scripts of Turms projects. Although the packaging and release process of each sub-project of Turms is open source, considering that if users need to search the warehouse for research, the learning efficiency is relatively low, so this article summarizes the generation methods of each product:</p><table><thead><tr><th>Projects</th><th>Products</th><th>Reference Methods</th></tr></thead><tbody><tr><td>turms-gateway</td><td>Fat/Uber JAR package (including dependencies)</td><td>1. Install <a href="https://adoptium.net/temurin/releases" target="_blank" rel="noreferrer">Temurin JDK 17</a><br>2. Install <a href="https://maven.apache.org/download.cgi" target="_blank" rel="noreferrer">Maven 3</a><br>3. Execute <code>mvn clean package -am -B -DskipUTs -DskipITs -P artifact-fat-jar -pl turms in the root directory of the Turms project -gateway --no-transfer-progress</code></td></tr><tr><td></td><td>Thin JAR package (the package does not include dependent packages)</td><td>1. Install <a href="https://adoptium.net/temurin/releases" target="_blank" rel="noreferrer">Temurin JDK 17</a><br>2. Install [Maven 3](https:/ /maven.apache.org/download.cgi)<br>3. In the root directory of the Turms project, execute the command <code>mvn clean package -am -B -DskipUTs -DskipITs -P artifact-thin-jar -pl turms-gateway - -no-transfer-progress</code></td></tr><tr><td></td><td>Docker image (linux/amd64)</td><td>1. Install <a href="https://docs.docker.com/engine/install" target="_blank" rel="noreferrer">Docker</a><br>2. Execute the command <code>docker build -- in the root directory of the Turms project rm=false -t &quot;www.mydomain.com/turms-im/turms-gateway:latest&quot; -f turms-gateway/Dockerfile .</code></td></tr><tr><td></td><td>Docker image (linux/arm64)</td><td>(cross-compilation)<br>If users use GitHub Action, they can directly reuse the <code>.github/workflows/publish-turms-gateway.yml</code> script under the Turms project for image packaging . <br>If the user does not use GitHub Action, and the system is not <code>linux/arm64</code>, cross-compilation is required. The specific method is as follows:<br>1. Install <a href="https://docs.docker.com/engine/install" target="_blank" rel="noreferrer">Docker</a><br>2. Install [buildx and QEMU](<a href="https://docs.docker" target="_blank" rel="noreferrer">https://docs.docker</a>. com/build/building/multi-platform)<br>3. In the root directory of the Turms project, execute the command <code>DOCKER_BUILDKIT=1 docker buildx build --platform linux/arm64 --rm=false -t &quot;www.mydomain.com /turms-im/turms-gateway:latest&quot; -f turms-gateway/Dockerfile .</code></td></tr><tr><td>turms-service</td><td>Fat/Uber JAR package (including dependencies)</td><td>1. Install <a href="https://adoptium.net/temurin/releases" target="_blank" rel="noreferrer">Temurin JDK 17</a><br>2. Install <a href="https://maven.apache.org/download.cgi" target="_blank" rel="noreferrer">Maven 3</a><br>3. Execute <code>mvn clean package -am -B -DskipUTs -DskipITs -P artifact-fat-jar -pl turms in the root directory of the Turms project -service --no-transfer-progress</code></td></tr><tr><td></td><td>Thin JAR package (the package does not include dependent packages)</td><td>1. Install <a href="https://adoptium.net/temurin/releases" target="_blank" rel="noreferrer">Temurin JDK 17</a><br>2. Install [Maven 3](https:/ /maven.apache.org/download.cgi)<br>3. In the root directory of the Turms project, execute the command <code>mvn clean package -am -B -DskipUTs -DskipITs -P artifact-thin-jar -pl turms-service - -no-transfer-progress</code></td></tr><tr><td></td><td>Docker image (linux/amd64)</td><td>1. Install <a href="https://docs.docker.com/engine/install" target="_blank" rel="noreferrer">Docker</a><br>2. Execute the command <code>docker build -- in the root directory of the Turms project rm=false -t &quot;www.mydomain.com/turms-im/turms-service:latest&quot; -f turms-service/Dockerfile .</code></td></tr><tr><td></td><td>Docker image (linux/arm64)</td><td>(cross-compilation)<br>If users use GitHub Action, they can directly reuse the <code>.github/workflows/publish-turms-service.yml</code> script under the Turms project for image packaging . <br>If the user does not use GitHub Action, and the system is not <code>linux/arm64</code>, cross-compilation is required. The specific method is as follows:<br>1. Install <a href="https://docs.docker.com/engine/install" target="_blank" rel="noreferrer">Docker</a><br>2. Install <a href="https://docs.docker.com/build/building/multi-platform" target="_blank" rel="noreferrer">buildx and QEMU</a><br>3. In the root directory of the Turms project, execute the command <code>DOCKER_BUILDKIT=1 docker buildx build --platform linux/arm64 --rm=false -t &quot;www.mydomain.com /turms-im/turms-service:latest&quot; -f turms-service/Dockerfile .</code></td></tr><tr><td>turms-admin</td><td>Static resources (such as HTML, JavaScript, CSS, etc.)</td><td>1. Install <a href="https://nodejs.org/en/download" target="_blank" rel="noreferrer">Node.js 18</a><br>2. In turms-admin Execute the command <code>npm i --no-optional &amp;&amp; npm run build</code> under the project</td></tr><tr><td></td><td>Docker image with Nginx service (linux/amd64)</td><td>1. Install <a href="https://docs.docker.com/engine/install" target="_blank" rel="noreferrer">Docker</a><br>2. Execute the command in the root directory of the Turms project<code> docker build --rm=false -t &quot;www.mydomain.com/turms-im/turms-admin:latest&quot; -f turms-admin/Dockerfile .</code></td></tr><tr><td></td><td>Docker image with Nginx service (linux/arm64)</td><td>(cross-compilation)<br>If users use GitHub Action, they can directly reuse <code>.github/workflows/publish-turms-admin.yml</code> under the Turms project The script performs image packaging. <br>If the user does not use GitHub Action, and the system is not <code>linux/arm64</code>, cross-compilation is required. The specific method is as follows:<br>1. Install <a href="https://docs.docker.com/engine/install" target="_blank" rel="noreferrer">Docker</a><br>2. Install [buildx and QEMU](<a href="https://docs.docker" target="_blank" rel="noreferrer">https://docs.docker</a>. com/build/building/multi-platform)<br>3. In the root directory of the Turms project, execute the command <code>DOCKER_BUILDKIT=1 docker buildx build --platform linux/arm64 --rm=false -t &quot;www.mydomain.com /turms-im/turms-admin:latest&quot; -f turms-admin/Dockerfile .</code></td></tr><tr><td>turms-client-dart</td><td>No packaging required</td><td>None</td></tr><tr><td>turms-client-java</td><td>JAR package</td><td>1. Install <a href="https://adoptium.net/temurin/releases" target="_blank" rel="noreferrer">Temurin JDK 17</a><br>2. Install [Maven 3](<a href="https://maven.apache" target="_blank" rel="noreferrer">https://maven.apache</a> .org/download.cgi)<br>3. Execute the command <code>mvn clean install</code> in the root directory of the turms-client-java project</td></tr><tr><td>turms-client-js</td><td>Static resources</td><td>1. Install <a href="https://nodejs.org/en/download" target="_blank" rel="noreferrer">Node.js 18</a><br>2. Execute the command <code>npm under the turms-client-js project i --no-optional &amp;&amp; npm run build</code></td></tr><tr><td>turms-client-swift</td><td>No packaging required</td><td>None</td></tr></tbody></table><h2 id="reference-configuration-of-linux-system" tabindex="-1">Reference Configuration of Linux system <a class="header-anchor" href="#reference-configuration-of-linux-system" aria-label="Permalink to &quot;Reference Configuration of Linux system&quot;">​</a></h2><h3 id="etc-security-limits-conf" tabindex="-1">/etc/security/limits.conf <a class="header-anchor" href="#etc-security-limits-conf" aria-label="Permalink to &quot;/etc/security/limits.conf&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">*        soft    nofile          1048576</span></span>
<span class="line"><span style="color:#A6ACCD;">*        hard    nofile          1048576</span></span></code></pre></div><ul><li><p>nofile is configured as the default maximum value of 1048576 (1024*1024), which is limited by <code>fs.nr_open</code>.</p><p>If users need a larger value, they can use the command <code>sudo sysctl -w fs.nr_open=2147483584</code> to modify the value of <code>fs.nr_open</code>, thereby increasing the upper limit of the maximum value of <code>nofile</code>.</p></li><li><p>The Turms server only needs a few threads to run normally, so the operation and maintenance personnel generally do not need to modify the <code>noproc</code> configuration specifically for the Turms server.</p><p>Regarding why the Turms server only needs a few threads to run normally, readers can read <a href="https://turms-im.github.io/docs/server/module/system-resource-management#%E7%BA%BF%E7%A8%8B%E6%A8%A1%E5%9E%8B" target="_blank" rel="noreferrer">Turms server thread model</a></p></li></ul><p>Reference documents:</p><ul><li><a href="https://www.kernel.org/doc/Documentation/sysctl/fs.txt" target="_blank" rel="noreferrer">https://www.kernel.org/doc/Documentation/sysctl/fs.txt</a></li></ul><h3 id="etc-sysctl-conf" tabindex="-1">/etc/sysctl.conf <a class="header-anchor" href="#etc-sysctl-conf" aria-label="Permalink to &quot;/etc/sysctl.conf&quot;">​</a></h3><p>The default values mentioned in the configuration below are from Ubuntu 20.04 LTS.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;"># Default value: 1629424. The total number of file descriptors that can be opened by all processes in the system. A socket needs to occupy a file descriptor</span></span>
<span class="line"><span style="color:#A6ACCD;">fs.file-max = 1629424</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 60. Use swap when the memory usage is less than 10%. Try to avoid using swap to reduce the wake-up soft interrupt process</span></span>
<span class="line"><span style="color:#A6ACCD;">vm.swappiness = 10</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 1024. Defines the maximum length of the SYN half-open connection queue. If this parameter is too large, it may intensify the effect of SYN flood attack</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_max_syn_backlog = 65536</span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 4096. Adjust the length of the accept queue. You can use the command: netstat -s | grep &quot;listen queue&quot; to see how many connections were dropped due to queue overflow. If there are continuous connections being discarded due to accept queue overflow, the backlog and somaxconn parameters should be increased</span></span>
<span class="line"><span style="color:#A6ACCD;">net.core.somaxconn=65536</span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 1. The SYN Cookie function is enabled only when the SYN semi-connection queue cannot hold a half-open connection. Syncookies can make half-open connections skip the SYN queue and establish connections directly, and can also mitigate SYN flood attacks</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_syncookies = 1</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 0. Do not cache metrics for closed TCP connections</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_no_metrics_save = 1</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 15. Control the number of timeout retransmissions of TCP. RFC1122 recommends that the corresponding timeout period should not be less than 100s, that is, at least 8</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_retries2 = 10</span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 6. When acting as a TCP client, the number of times to retry sending SYN to initiate a handshake. It will wait 1, 2, 4 seconds before each retry (7 seconds in total). When communicating in the intranet, the number of retries can be appropriately reduced to expose errors to the application as soon as possible</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_syn_retries = 3</span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 5. When used as a TCP server, the number of retries for SYN+ACK packets. It will wait for 1, 2, 4, 8, 16, and 32 seconds before each retry (a total of 63 seconds is required), and if the last retry still does not receive ACK, the connection will be closed</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_synack_retries = 5</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 1. Enable selection confirmation, so that TCP only resends lost TCP segments, without sending all unacknowledged TCP segments</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_sack = 1</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 0. Set to 0 to drop the connection when the accept queue overflows. The ACK is retransmitted by the TCP client, and the server tries to put the connection into the accept queue again. Discarding the connection can improve the success rate of connection establishment. Only when it is very sure that the accept queue will overflow for a long time can it be set to 1. By sending an RST reset message to the client, the client will be notified as soon as possible that the connection has failed</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_abort_on_overflow = 0</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 65536. The maximum number of TCP connections in the system are not associated with any process (when the process calls the close function to close the connection, no matter whether the connection is in the FIN_WAIT1 state or is actually closed), if the number of orphan connections is greater than it, the newly added Orphan connections will no longer wave four times, but will directly send RST reset messages to force close</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_max_orphans = 65536</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 2. Enable TIME_WAIT reuse, so that new connections can reuse ports in TIME_WAIT state</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_tw_reuse = 1</span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 1. In order to make tcp_tw_reuse take effect, the timestamps parameter needs to be set to 1 (tcp_timestamps should also be enabled on the peer). This option provides a more accurate method of calculating the round trip time (Round Trip Time) RTT between the two communicating parties</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_timestamps = 1</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 65536. The system maintains the maximum number of TIME_WAIT sockets simultaneously. When the number of TIME_WAIT connections exceeds this parameter, the newly closed connection will no longer experience TIME_WAIT and will be closed directly</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_max_tw_buckets = 65536</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 60. Specify how long the TCP connection with the state of FIN_WAIT_2 is kept</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_fin_timeout = 30</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Keep the default configuration related to TCP keepalive, because the application layer of Turms has its own set of heartbeat mechanism</span></span>
<span class="line"><span style="color:#A6ACCD;"># net.ipv4.tcp_keepalive_probes = ...</span></span>
<span class="line"><span style="color:#A6ACCD;"># net.ipv4.tcp_keepalive_intvl = ...</span></span>
<span class="line"><span style="color:#A6ACCD;"># net.ipv4.tcp_keepalive_time = ...</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value 1. Enable TCP Fast Open, the client can carry the request in the first SYN message to save 1 RTT time</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_fastopen = 3</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default value: 1000. When the network card receives data packets faster than the kernel can process them, there will be a backlog to cache these data packets. This parameter represents the maximum value of the queue. When the backlog overflows, the kernel will drop packets</span></span>
<span class="line"><span style="color:#A6ACCD;">net.core.netdev_max_backlog=65536</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Define the maximum value that the receiving window can use, which can be adjusted according to the BDP value</span></span>
<span class="line"><span style="color:#A6ACCD;">net.core.rmem_max = 16777216</span></span>
<span class="line"><span style="color:#A6ACCD;">net.core.wmem_max = 16777216</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># [low, pressure, high], the unit is page (4096 bytes)</span></span>
<span class="line"><span style="color:#A6ACCD;"># low: When the total used memory of all TCP connections is lower than this value, TCP memory will not be automatically adjusted</span></span>
<span class="line"><span style="color:#A6ACCD;"># pressure: When the total used of all TCP connections is greater than pressure, the kernel starts to adjust the buffer size</span></span>
<span class="line"><span style="color:#A6ACCD;"># high: When the total used memory of all TCP connections is greater than this value, the kernel will no longer allocate new memory for TCP and will not establish new connections. It should be ensured that the dynamic adjustment upper limit of the buffer reaches the bandwidth-delay product</span></span>
<span class="line"><span style="color:#A6ACCD;"># No custom configuration, use the default value automatically calculated by the system</span></span>
<span class="line"><span style="color:#A6ACCD;"># net.ipv4.tcp_mem = ...</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># [min, default, max], the unit is byte</span></span>
<span class="line"><span style="color:#A6ACCD;"># min: Specifies the minimum memory reserved for receiving buffers for each TCP connection. Even in pressure mode, TCP connections will reserve at least this part of memory for receiving buffers</span></span>
<span class="line"><span style="color:#A6ACCD;"># default: Specifies the initial memory size of each TCP connection for receiving buffers</span></span>
<span class="line"><span style="color:#A6ACCD;"># max: Specifies the maximum memory used for receiving buffers per TCP connection</span></span>
<span class="line"><span style="color:#A6ACCD;"># If the buffer is too small, the TCP throughput will be reduced, and the network bandwidth cannot be used efficiently, resulting in increased communication delay; if the buffer is too large, the memory usage of the TCP connection will be high and the bottleneck limited by the bandwidth and delay product will cause memory loss. waste</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_rmem = 4096 87380 16777216</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_wmem = 4096 87380 16777216</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 1. Enable the adjustment function of the receive buffer</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_moderate_rcvbuf = 1</span></span>
<span class="line"><span style="color:#A6ACCD;"># Default: 1. TCP uses 16 bits to record the window size, and the maximum value can be 65535B. If this value is exceeded, the tcp_window_scaling mechanism needs to be enabled</span></span>
<span class="line"><span style="color:#A6ACCD;">net.ipv4.tcp_window_scaling = 1</span></span></code></pre></div><p>Once configured, execute <code>sudo sysctl -p</code> to load the latest configuration of sysctl.</p><p>Special mention is: we are in <a href="https://turms-im.github.io/docs/server/module/system-resource-management#%E5%8F%AF%E6%8E%A7%E5%86%85%E5%AD%98-managed-memory-%E7%9A%84%E4%BD%BF%E7%94%A8" target="_blank" rel="noreferrer">system resource management</a> mentioned that the Turms server will reserve part of the memory for the system Kernel, this part of memory mainly refers to the buffer of the above-mentioned TCP connection.</p><h3 id="initial-congestion-window-initcwnd-configuration" tabindex="-1">Initial congestion window (initcwnd) configuration <a class="header-anchor" href="#initial-congestion-window-initcwnd-configuration" aria-label="Permalink to &quot;Initial congestion window (initcwnd) configuration&quot;">​</a></h3><p>Keep the default value: 10MSS.</p><p>Reference documents:</p><ul><li><a href="https://www.kernel.org/doc/Documentation/sysctl/net.txt" target="_blank" rel="noreferrer">https://www.kernel.org/doc/Documentation/sysctl/net.txt</a></li><li><a href="https://www.kernel.org/doc/Documentation/networking/ip-sysctl.txt" target="_blank" rel="noreferrer">https://www.kernel.org/doc/Documentation/networking/ip-sysctl.txt</a></li></ul>`,28),r=[o];function l(i,c,p,d,u,h){return s(),t("div",null,r)}const y=e(a,[["render",l]]);export{f as __pageData,y as default};
